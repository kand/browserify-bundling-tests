# Unsuccessfully Requiring with Browserify

To break:
```bash
$ npm install
$ npm run failure
```

Why is ```bundle-2``` not able to resolve ```bundle.one``` from the provided
requirement ```-x ./build/bundle.one.js```?
